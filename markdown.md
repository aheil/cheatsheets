# Displaying links (or text looking like alink) as text 

Simply add a zero width whitespace `&#8203;` after the colon.

Example `
* `ASP.NET` redners sometimes as [ASP.NET]() 
* `ASP.&#8203;NET` redners always as ASP.&#8203;NET
